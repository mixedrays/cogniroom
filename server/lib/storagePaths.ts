/**
 * @deprecated Import from `@modules/storage/paths` instead. Kept as a
 * re-export during the browser-storage migration so existing server imports
 * keep working.
 */
export { storagePaths } from "@modules/storage/paths";
